{% extends 'front.html.twig' %}

{% block front %}
    <div class="row">
        <div class="col-12">
            <h3 class="mb-5">Invités</h3>
            <div class="guests d-flex flex-column">
                {% for row in guests %}
                    <div class="guest py-5 d-flex justify-content-between align-items-center">
                        <h4>{{ row.guest.name }} ({{ row.mediaCount }})</h4>
                        <a href="{{ path('guest', {id: row.guest.id}) }}">découvrir</a>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

