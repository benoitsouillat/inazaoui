{% extends 'admin.html.twig' %}

{% block admin %}
<div class="d-flex justify-content-between align-items-center">
    <h1>Invités</h1>
    <a href="{{ path('admin_guest_add') }}" class="btn btn-primary">Ajouter</a>
</div>
    <table class="table">
        <thead>
        <tr>
            <th>Nom & Prénom</th>
            <th>Nom d'utilisateur</th>
            <th>Email</th>
            <th>Description</th>
            <th>Activé</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for guest in guests %}
            <tr>
                <td>{{ guest.name }}</td>
                <td>{{ guest.username }}</td>
                <td>{{ guest.email }}</td>
                <td>{{ guest.description }}</td>
                <td>{{ guest.active ? "✅" : "❌" }}</td>
                <td>
                    <div class="d-flex flex-column align-items-start gap-2">
                        <a href="{{ path('admin_guest_update', {id: guest.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
                        <a href="{{ path('admin_guest_toggle', {id: guest.id}) }}" class="btn btn-sm {{ guest.active ? "btn-dark" : "btn-success" }}">{{ guest.active ? "Désactiver" : "Activer" }}</a>
                        <a href="{{ path('admin_guest_delete', {id: guest.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
